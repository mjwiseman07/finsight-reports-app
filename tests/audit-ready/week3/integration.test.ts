import { describe, it, expect } from 'vitest';

describe.skip('week3 integration (requires live Supabase + Bedrock)', () => {
  it('end-to-end: upload → parse → classify → rows visible', async () => {
    expect(true).toBe(true);
  });

  it('cap breach falls back to Haiku', async () => {
    expect(true).toBe(true);
  });

  it('cross-engagement RLS blocks unauthorized read', async () => {
    expect(true).toBe(true);
  });
});
