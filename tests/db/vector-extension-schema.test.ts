import { describe, it } from "vitest";

// Q8d regression test placeholder.
// The migration itself contains a DO $$ ... $$ assertion that fails apply
// if `vector` is not in `extensions`. That is the primary guard.
// This test file exists as a future hook for an integration check.

describe.skip("Q8d vector extension schema (integration hook)", () => {
  it.todo("assert vector is in extensions schema via helper view");
});
